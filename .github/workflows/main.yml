name: Build

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run linting
        run: npm run lint

      - name: Run tests
        run: npm run test

      - name: Build app
        run: npm run build:app
